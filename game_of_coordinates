#include <iostream>
#include<bits/stdc++.h>
using namespace std;

int main() {
	int gcd;
    int n,m;
	 int a,b;
	  cin>>n>>m;
	  cin>>a>>b;
	  if(abs(__gcd(a,b))==abs(__gcd(n,m)))
	  cout<<"yes"<<"\n";
	 else
	  cout<<"no"<<"\n";
	return 0;
}
